<h2>Photobomb!</h2>

<div class="row">
  <% @photos.each do |photo| %>
    <div class="col-md-3">
      <%= image_tag photo.picture.url(:medium), :class => 'display-image' %>
    </div>
  <% end %>
</div>
